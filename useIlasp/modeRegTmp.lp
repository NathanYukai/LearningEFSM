#modeh(delta(const(st),var(time),var(in),const(st),const(cond_id),const(cond_id),const(reg_value))).

#constant(cond_id,0).
#constant(cond_id,1).
#constant(cond_id,2).


#modeb(1,input(var(time),var(in))).

%add register_value constant!#constant(st,state0).
#constant(st,state1).
#constant(reg_value,0).
#constant(reg_value,1).
